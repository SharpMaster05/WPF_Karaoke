<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="PrimaryText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource Foreground}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="FontFamily" Value="{DynamicResource PrimaryFont}"/>
        <Setter Property="FontStretch" Value="Expanded"/>
    </Style>

    <Style x:Key="WindowTitle" TargetType="TextBlock" BasedOn="{StaticResource PrimaryText}">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="Margin" Value="10,0,0,0"/>
    </Style>

    <Style x:Key="ButtonContent" TargetType="TextBlock" BasedOn="{StaticResource PrimaryText}">
        <Setter Property="Foreground" Value="{DynamicResource Background}"/>
    </Style>

    <Style x:Key="Header" TargetType="TextBlock" BasedOn="{StaticResource PrimaryText}">
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="30"/>
        <Setter Property="Margin" Value="20"/>
    </Style>

    <Style x:Key="ItemsControlRows" TargetType="TextBlock" BasedOn="{StaticResource PrimaryText}">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="ExtraLight"/>
    </Style>
    
</ResourceDictionary>